

class Character {
    minFireTime: number
    maxFireTime: number
    constructor(minFireTime: number, maxFireTime: number) {
        this.minFireTime = minFireTime
        this.maxFireTime = maxFireTime
    }
}

export default Character;